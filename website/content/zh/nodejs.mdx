---
title: "Node.js SDK — @wechatbot/wechatbot"
description: "TypeScript 优先的微信 iLink Bot SDK，中间件管道，可插拔存储，41 个测试。"
toc:
  - label: "安装"
    href: "#install"
  - label: "快速开始"
    href: "#quick-start"
  - label: "配置"
    href: "#configuration"
  - label: "中间件"
    href: "#middleware"
  - label: "媒体"
    href: "#media"
  - label: "存储"
    href: "#storage"
  - label: "事件"
    href: "#events"
  - label: "消息构建器"
    href: "#message-builder"
  - label: "API 参考"
    href: "#api-reference"
  - label: "项目结构"
    href: "#structure"
---

Node.js SDK 是功能最丰富的实现。**42 个源文件**，**10 个模块**，**41 个测试**，**零运行时依赖**。需要 Node.js ≥ 22。

<Section id="install" title="安装">

```bash
npm install @wechatbot/wechatbot
```

</Section>

<Section id="quick-start" title="快速开始">

```typescript
import { WeChatBot } from '@wechatbot/wechatbot'

const bot = new WeChatBot()
await bot.login()

bot.onMessage(async (msg) => {
  await bot.sendTyping(msg.userId)
  await bot.reply(msg, `Echo: ${msg.text}`)
})

await bot.start()
```

</Section>

<Section id="configuration" title="配置">

```typescript
const bot = new WeChatBot({
  storage: 'file',            // 'file' | 'memory' | 自定义 Storage
  storageDir: '~/.wechatbot',
  logLevel: 'info',           // 'debug' | 'info' | 'warn' | 'error' | 'silent'
  loginCallbacks: {
    onQrUrl: (url) => renderQrCode(url),
    onScanned: () => console.log('已扫码！'),
    onExpired: () => console.log('已过期...'),
  },
})
```

</Section>

<Section id="middleware" title="中间件">

Express/Koa 风格的可组合中间件管道，在消息处理器之前运行。

```typescript
import {
  WeChatBot, loggingMiddleware, rateLimitMiddleware,
  typeFilterMiddleware, filterMiddleware,
} from '@wechatbot/wechatbot'

const bot = new WeChatBot()

bot.use(loggingMiddleware(bot.logger))          // 记录每条消息
bot.use(rateLimitMiddleware({ maxMessages: 10, windowMs: 60_000 }))
bot.use(typeFilterMiddleware('text', 'image'))   // 仅处理文本和图片
bot.use(filterMiddleware(/^\/\w+/))              // 仅处理命令

// 自定义中间件
bot.use(async (ctx, next) => {
  const start = Date.now()
  await next()
  console.log(`处理耗时 ${Date.now() - start}ms`)
})
```

</Section>

<Section id="media" title="媒体处理">

完整的图片/文件/语音/视频支持，AES-128-ECB CDN 加密。

```typescript
bot.onMessage(async (msg) => {
  if (msg.type === 'image' && msg.images[0]?.media) {
    const data = await bot.downloadMedia(msg.images[0].media, msg.images[0].aeskey)
    await writeFile('photo.jpg', data)
  }

  if (msg.type === 'voice' && msg.voices[0]?.text) {
    await bot.reply(msg, `你说了: "${msg.voices[0].text}"`)
  }
})
```

</Section>

<Section id="storage" title="可插拔存储">

```typescript
const bot1 = new WeChatBot({ storage: 'file' })     // 文件存储（默认）
const bot2 = new WeChatBot({ storage: 'memory' })   // 内存存储
const bot3 = new WeChatBot({ storage: new RedisStorage() }) // 自定义
```

</Section>

<Section id="events" title="事件">

```typescript
bot.on('login', (creds) => console.log(`已登录: ${creds.accountId}`))
bot.on('session:expired', () => console.log('会话过期'))
bot.on('session:restored', (creds) => console.log('已恢复'))
bot.on('error', (err) => console.error(err))
```

</Section>

<Section id="message-builder" title="消息构建器">

```typescript
const msg = bot.createMessage(userId)
  .text("这是你的报告：")
  .file({ media: cdnRef, fileName: 'report.pdf', size: 542188 })
  .build()

await bot.sendMessage(msg)
```

长文本自动在自然边界处分片（段落 → 换行 → 空格 → 2000字符硬切）。

</Section>

<Section id="api-reference" title="API 参考">

<ComparisonTable
  title="核心方法"
  headers={['方法', '描述', '返回']}
  rows={[
    ['new WeChatBot(opts?)', '创建实例', 'WeChatBot'],
    ['bot.login(opts?)', '二维码登录', 'Promise<Credentials>'],
    ['bot.start()', '启动轮询', 'Promise<void>'],
    ['bot.stop()', '优雅停止', 'void'],
    ['bot.onMessage(handler)', '注册处理器', 'this'],
    ['bot.reply(msg, text)', '回复消息', 'Promise<void>'],
    ['bot.send(userId, text)', '发送消息', 'Promise<void>'],
    ['bot.sendTyping(userId)', '显示输入中', 'Promise<void>'],
    ['bot.downloadMedia(media)', '从 CDN 下载', 'Promise<Buffer>'],
    ['bot.use(middleware)', '添加中间件', 'this'],
  ]}
/>

</Section>

<Section id="structure" title="项目结构">

```
nodejs/src/
├── core/           ← 客户端、类型化事件、错误层次
├── transport/      ← HTTP 客户端（重试 + 超时）
├── protocol/       ← iLink API 调用 + 线路类型
├── auth/           ← 二维码登录 + 凭证持久化
├── messaging/      ← 轮询器、发送器、输入状态、上下文存储
├── media/          ← AES-128-ECB 加密、CDN 上传/下载
├── middleware/      ← 引擎 + 4 个内置中间件
├── message/        ← 解析器、构建器、友好类型
├── storage/        ← 可插拔（文件、内存、接口）
├── logger/         ← 结构化分级日志
└── index.ts        ← 公开导出
```

</Section>
